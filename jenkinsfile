pipeline {
     agent any
     stages{
        stage('Run tests interface') {
            steps{
                    git credentialsId:'github_login' ,url:'https://gitlab.com/rodrigo.qa/sicred-automation-tests-web'
                    bat 'mvn test -Dgroups="add-cliente,excluir-cliente"'
                }
            }

         }
    }
